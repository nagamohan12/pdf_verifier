 <tr>
  <td><%= user.username %></td>
      <td><%= user.email %>
  </td>
      <td><%= user.created_at %></td>
  <td>
    <%= link_to image_tag('../assets/edit-icon.png',title: 'Edit User'), edit_admin_user_path(user)%>
    <% unless current_user == user%>
      <%= link_to  image_tag('../assets/delete-user-icon.png',title: 'Delete User'), admin_user_path(user), :method => :delete, :data => { :confirm => 'Are you sure?' } %>
    <%end%>
  </td>
</tr>